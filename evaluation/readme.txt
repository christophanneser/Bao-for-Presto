Invoke scripts in this folder from parent folder to save the generated figures to the correct location.

Make the following env variables available:
export DB_PASSWORD=;
export SCALE_FACTOR=;
export DB_USER=;
export DB_NAME=;
export REPEATS=1;
export DB_SCHEMA=;
export DB_HOST=;